import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddWatchListButton = () => {
  return (
    <View>
      <Text>AddWatchListButton</Text>
    </View>
  );
};

export default AddWatchListButton;

const styles = StyleSheet.create({});
